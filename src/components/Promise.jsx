import React from 'react'

function Promise({ Promise }) {
    return (
        <div style={{ width: 300, height: 150, background: 'gray', color: '#fff' }}>
            <h2>{Promise}</h2>
        </div>

    )
}

export default Promise